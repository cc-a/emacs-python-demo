((nil . (
	 (project-uses-black . t)
	 (elpy-test-runner . elpy-test-pytest-runner)
	 ;; further customisation of test runner e.g. if you're using poetry
	 ;; (elpy-test-pytest-runner-command . ("poetry" "run" "pytest"))
	 )
      )
 )
